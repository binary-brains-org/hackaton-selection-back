package com.hakathon.project.controller;

import com.hakathon.project.model.Investment;
import com.hakathon.project.model.Wallet;
import lombok.AllArgsConstructor;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

@RestController
@AllArgsConstructor
public class InvestmentController {
    /*@PostMapping("/transferMoney/{user_id}")
    public Investment transferMoney(@PathVariable String user_id, @RequestBody Investment investment){

    }*/
}
